# STM32F750V8 FreeRTOS 嵌入式工程问题分类速查（当前代码版）

> 本文档按「工程问题根因类型 → 本项目实际案例」组织，**代码状态以当前开环底盘为准**。
> 旧闭环底盘（PID 位置闭环 + `Move_To_Target_area`）踩过的问题保留为「历史教训」，统一标注 **旧闭环**，不代表当前代码。
> 出处：`.agents/skills/embedded/SKILL.md`（Codex 整理版）＋ 对照当前代码核实修订，2026-08-01。

---

## 〇、当前系统结构速览

> 这部分描述的是**现在的代码**，所有接口/参数以此为准。后续分类章节里"当前问题"引用的都是这里的结构。

### 底盘控制（开环速度方案，非位置闭环）

| 项 | 值 | 出处 |
|----|----|----|
| 控制周期 | `OPEN_LOOP_PERIOD_MS = 5ms`（`vTaskDelayUntil`） | `MOTOR/motor_control.c:38` |
| 运行位置 | 主流程任务内直接调用（**无独立底盘任务**） | `task/main_task.c` → `APP/task_flow.c` |
| 距离接口 | `Chassis_MoveByDistance(vx, vy, target_angle, distance_cm)` | `motor_control.c:416` |
| 速度设置 | `Chassis_OpenLoop_SetSpeed(vx_world, vy_world, target_angle)` | `motor_control.c:168` |
| 帧锁定 | `Chassis_OpenLoop_SetSpeedFrame(…, speed_frame_angle, target_angle)`，平移速度不随车头转 | `motor_control.c:149` |
| 漂移过弯 | `Chassis_BlendSpeedAngle` / `Chassis_HoldSpeedAngle` / `Chassis_DriftStraightTurn` | `motor_control.c:726/700/752` |
| 移动波形 | 正弦加减速（`Chassis_SINAccel`）→ 匀速 → 正弦减速 → 停车 | `motor_control.c:259` |

距离换算：`distance_cm × PULSE_PER_CM` → 目标脉冲，`Chassis_MoveByPulseInternal` 边跑边比对软件脉冲，接近目标脉冲提前进入减速。

### 电机协议（UART3，X42S ×4）

| 帧 | 格式 | 当前状态 |
|----|------|---------|
| 多机速度包 | `00 AA 00 25` + 4×8B 电机命令 + `6B`（37B） | **主力路径**，`send_speed_data_all` |
| 同步触发 | `00 FF 66 6B` | 每次速度命令后必发，`Send_motor_together` |
| 读位置 | `id 36 6B`，逐电机轮询，间隔 3ms | `Motor_ReadPulseSnapshot`（读脉冲快照） |
| 位置模式 | 0xFD 13B / `Motor_setposition` | **死代码**，跑图不再使用 |
| 0.1RPM 精调 | `Motor_setspeed_fine`，驱动器端已存 scale10 配置 | 转向精调用 |

- `Motor_setspeed(vy, vx, vw)` → `Motor_Send_Speed_together` 打包 → `send_speed_data_all`（0xAA 37B）+ `Send_motor_together`（4B 触发）。
- 发送保护：`uart3WriteBuf` 先等 `huart3.gState == READY`，超 5ms 返回 `HAL_TIMEOUT`（丢帧计数 `chassis_odom_tx_fail_count`）。`MOTOR/motor.c:58`

### 航向 / 转向

| 项 | 值 | 出处 |
|----|----|----|
| 航向 PID | `Gyro_Pid` Kp=2.1 Ki=0 Kd=0.5，输出 ±120 | `MOTOR/imu_control.c:11-15` |
| 航向计算 | `Direction_Calibration_turn(target)`（内部最短路径归一化） | `imu_control.c:72` |
| 转向到位 | `Chassis_TurnToAngle(target, timeout)` | `motor_control.c:589` |
| 到位阈值 | `CHASSIS_TURN_THRESHOLD = 0.1°`，连续 `SETTLE_COUNT = 7` 次（≈50ms） | `motor_control.c:17-18` |
| 转向加减速 | `ACCEL_DELTA=2.0` / `DECEL_DELTA=4.0`（RPM/5ms），`RPM_MAX=150` | `motor_control.c:20-29` |
| 最小转向速度 | `CHASSIS_TURN_MIN_SPEED = 2.0`（仅转向用） | `motor_control.c:19` |
| 漂移补偿 | `Yaw_DriftCorrect(expected_current, target)`，每次转向前补偿 IMU 累积漂移 | `imu_control.c:86` |
| 角速度双环 | `CHASSIS_TURN_USE_DOUBLE_LOOP = 0`（当前实验单环） | `motor_control.c:15` |

### 里程计（软件脉冲积分）

- 4 轮 `odom_last_rpm_x10 × 经过时间` 积分 → 段内软件脉冲 → `Chassis_WorldCommitSegment(segment_yaw)` 做体坐标→世界坐标旋转变换，累加进 `car.actual_x/y`；`car.actual_w` 由 yaw 增量累加。
- 段前后 `Motor_ReadPulseSnapshot` 对比实际脉冲增量与软件估算，打印到 HMI（`Chassis_EndSegment`）。
- 世界坐标写处用 `taskENTER_CRITICAL` 保护（`Chassis_WorldCommitSegment` / `Chassis_WorldPoseReset`）。

### 任务（FreeRTOS）

| 任务 | 优先级 | 栈 | 说明 |
|------|--------|----|------|
| IMU_Task | 7 | 128 | 5ms `vTaskDelayUntil`，`IMU_Process` 解析 |
| Start_Task | 5 | 256 | 初始化完成后自删 |
| Main_Task | 5 | 512 | 主流程 `Flow_RunCurrent`（含开环底盘调用） |
| HMI_Task | 4 | 384 | 串口屏 |

### 机械臂（UART7，Z 升降 / Y 伸缩）

- `POSTION_init` → DMA+IDLE 接收（`UART7_RxRestart` 恢复机制，RXNE 中断已关）。
- `Z_SetHeight(high)` / `Y_SetLength(len)`：发绝对位置命令后**阻塞等待** `BIT != finish`，带 4000ms 超时（`Z_MAX_TIMEOUT_MS`）。超时记录 `z_debug_last_timeout`。
- 上电后等待 `UART7_COLD_BOOT_WAIT_MS=500ms` 再初始化，避免冷启动污染 DMA 状态。

---

## 一、总览表：工程问题类型 → 典型症状

| 根因类型 | 一句话原理 | 本项目典型症状 |
|---------|-----------|---------------|
| ① 共享总线写路径冲突 | 单 UART 同时只能一个发送方 | 单轮慢半拍、发送超时丢帧、5ms 周期带宽吃紧 |
| ② DMA 缓冲生命周期 & 内存 | DMA 异步读 buffer；栈溢出表现随机 | 偶发乱帧、崩溃静默挂死、栈相关不稳定 |
| ③ 竞态 & 共享状态写入顺序 | A 完成后 B 才生效，窗口内切换即错 | 脉冲快照超时、Z 到位标志错位、世界坐标撕裂 |
| ④ 异步命令 & 外设时序 | 配置/复位命令异步，硬件需时间 | IMU 归零直走弯、UART7 冷启动污染、Z 阻塞超时 |
| ⑤ 控制环调参 & 角度/周期 | 角度最短路径、周期与参数联动 | 转向转圈、5ms 周期超跑、到位不准 |
| ⑥ 任务优先级 & FreeRTOS 调度 | 优先级抢占影响共享资源访问 | 电机目标值撕裂、IMU 解析被压 |

---

## ② DMA 缓冲生命周期 & 栈/内存

**根因**：`HAL_UART_Transmit_DMA` 立刻返回，DMA 在后台异步读 buffer；局部变量 buffer 在函数返回后栈帧销毁 → 偶发乱帧。栈溢出时 FreeRTOS 不会立刻崩溃，覆盖相邻内存 → 随机难复现行为。

### 当前问题

| # | 症状 | 原因 | 修复 |
|---|------|------|------|
| 2.1 | UART3 偶发乱帧 | 传给 DMA 的发送 buffer 是局部变量 | 确认全部 `static`/全局：`all_send[37]`、`data[4]`、`sendmotor_coordination_data[3]`、`USART7_senddata[128]` |
| 2.2 | DMA 读到垃圾 | buffer 放 DTCM —— M7 DTCM 核内耦合，DMA 不可达 | DMA 缓冲不得放 ITCM/DTCM |
| 2.3 | 崩溃后静默挂死 | 四个 Fault Handler 都是空 `while(1)` | 加 HardFault 跳板保存 PC |
| 2.4 | 栈相关随机不稳定 | 栈分配不足 | 现状：Main 512 / Start 256 / HMI 384 / IMU 128；改栈后重测 |

### 历史教训（旧闭环）

- `Start_Task` 栈 128 → 256：加入 `POSTION_init`（含 `HAL_UARTEx_ReceiveToIdle_DMA` 大调用链）后偶发上电不启动。规则：初始化函数调链深时栈至少 256。
- 崩溃分析（Keil）：HardFault 断点 → R13/R14 → LR 判 PSP/MSP → SP 第 7 个字（偏移 0x18）为 PC。CFSR：`NOCP`=FPU 未开用浮点、`UNDEFINSTR`=PC 跳到数据（栈破坏）、`DIVBYZERO`=浮点除零、`IMPRECISERR`=缓冲溢出。

### HardFault 跳板（推荐保留）
```c
void HardFault_Handler(void) {
    __asm volatile(
        "tst lr, #4        \n"
        "ite eq             \n"
        "mrseq r0, msp     \n"
        "mrsne r0, psp     \n"
        "b HardFault_Handler_C \n"
    );
}
void HardFault_Handler_C(uint32_t *stack) {
    volatile uint32_t pc = stack[6];
    volatile uint32_t cfsr = *(volatile uint32_t*)0xE000ED28;
    __BKPT(0);
    while(1);
}
```

---

## ③ 竞态 & 共享状态写入顺序

**根因**：多任务/ISR 环境下，操作依赖「A 完成后 B 才生效」，两者间的代码窗口就是竞态窗口，任何任务切换落在窗口内都出错。标志晚写、读取早读。

### 当前问题

| # | 症状 | 原因 | 修复 |
|---|------|------|------|
| 3.1 | 脉冲快照超时（HMI 报 "motor pulse start lost"） | `motor_check.flag_finish` 由 UART3 解析路径置位、`Motor_ReadPulseSnapshot` 清零并等 0x0F；任一电机 3ms 轮询丢帧则等不到 | 保持 `volatile`；轮询加 Delay_ms(3) 已防粘包；仍失败则重试/放弃并打日志 |
| 3.2 | Z 到位标志错位 | `Z_POSTION.BIT` / `Telescopic_POSTION.BIT` 由 UART7 解析写、`Z_SetHeight` 阻塞读 | 只在本任务阻塞等，解析在中断路径，保持单写单读；确认无第二个写者 |
| 3.3 | 世界坐标撕裂 | `car.actual_x/y/w` 由 `Chassis_WorldCommitSegment` 写，其他任务读 | 写处包 `taskENTER_CRITICAL`（已做），读处用 `Chassis_OdomGetSegmentSnapshot` |
| 3.4 | 电机 4 目标值中途被改 | `motor1..4.target_angle` 由 `Motor_Action_Calculate_target` 一次性写 4 个 | 已用 `__disable_irq()/__enable_irq()` 包裹 |

### 历史教训（旧闭环）

- `flag_finish` 竞态 → 3 号电机反馈丢失（实测验证）：`Move_To_Target_area` 的先 `Motor_SetZero()` 再设 `MOTOR_ACTIONFALG=Incomplete` 顺序错，底盘任务在窗口内看到旧 finish 直接返回。**该逻辑与 `Move_To_Target_area` 一起已删除**。
- double-move 分段走：finish 块里 `Motor_SetZero()` 清零制造假误差 → 再次驱动。已随闭环架构删除。
- `MOTOR_ACTIONFALG` 当前只剩初始化赋值（`motor.c:12`），不再驱动流程，属遗留。

---

## ④ 异步命令 & 外设初始化时序

**根因**：向外设发「配置/复位」命令是异步的，UART 帧发出后硬件需要时间执行，立即读状态拿到旧值。初始化顺序、接收机制选择也影响上电行为。

### 当前问题

| # | 症状 | 原因 | 修复 |
|---|------|------|------|
| 4.1 | IMU 归零后直行走弯 | `Imu_setZero()` 发完命令立刻用 `imu.yaw`，归零需 100~200ms | 归零后等待 200ms 再动作（测试参数已固化 `IMU_STATIC_ZERO_WAIT_MS=200` / `IMU_STRAIGHT_ZERO_WAIT_MS=200`） |
| 4.2 | UART7 冷启动接收污染 | 上电时机械臂驱动器还在发串口活动，污染 DMA 状态 | `UART7_COLD_BOOT_WAIT_MS=500ms` 后再 `POSTION_init`（`task/start_task.c:21`） |
| 4.3 | Z 到位等待永远不结束 | 收到到不位帧 | `Z_SetHeight` 带 4000ms 超时放弃，看 `z_debug_last_timeout` / `z_debug_last_wait_ms` 确认是否超时 |
| 4.4 | DMA 接收进坏状态 | 总线错误/溢出破坏 HAL 状态机 | `UART7_RxRestart` / `UART3_RxRestart`：Abort → 清 ORE 等标志 → 复位 RxState → 重新 `ReceiveToIdle_DMA` |

### 历史教训（旧闭环）

- **同一 UART 只能用一种接收机制**：`Telescopic_Init` 在 UART7 开 RXNE，`POSTION_init` 又用 DMA+IDLE，上电噪声触发 RXNE 破坏 HAL 状态机，`Z_SetHeight` 永远等不到 finish。已删掉 RXNE，当前 UART7 只走 DMA+IDLE（`UART7_RxRestart` 里也显式关 RXNE）。
- `vTaskSuspend` 不挂起 DMA：旧 `Set_chassis_able` 挂起底盘任务后，定时返回 DMA 仍覆盖刚清零的 `actual_angle`。**`Set_chassis_able` 已从代码删除**，现在机械臂动作前用 `Motor_setspeed(0,0,0)` 明确停车。

---

## ⑤ 控制环调参 & 角度/周期

**根因**：角度是循环量必须走最短路径；控制周期与路径参数强联动；到位边界附近行为取决于阈值/死区/min 速度。

### 当前问题

| # | 症状 | 原因 | 修复 |
|---|------|------|------|
| 5.1 | 转向朝相反方向转一整圈 | 目标角 270° 与 `normalize_angle(imu.yaw)`=-90° 是同一方向，naive 差值 360° | 所有角度差走最短路径：`Direction_Calibration_turn`、`getAngleZ`、`MoveTurnOnce`/`BlendSpeedAngle` 的 `angle_delta` 已 while 归一化；**新增角度计算也必须归一化** |
| 5.2 | 转向到不了目标 / 转不到位 | 阈值 0.1° 过严、MIN_SPEED=2.0 方向选择 | 调 `CHASSIS_TURN_THRESHOLD` / `CHASSIS_TURN_SETTLE_COUNT`；确认 `MIN_SPEED` 只作用于转向 |
| 5.3 | 5ms 周期超跑 | 某周期执行超过 5ms（读脉冲 4×3ms + 发送 41B≈3.6ms 挤在一起） | 看 `chassis_period_overrun_count` / `chassis_period_max_ms`；超跑说明该调用点不能放进 5ms 节奏 |
| 5.4 | 短距离到位不准 | 速度档太高，加减速段占满整个行程（正弦 ramp 未走完就触发减速） | 短距离按速度档位选低档（5cm→20、10cm→40、20cm→80、40cm+→160）；`PULSE_PER_CM` 是机械常数，**改速度不需要重标定系数** |
| 5.5 | 起步/停车打滑 | 速度突变 | 用 `Chassis_SINAccel` 正弦加减速（当前默认） |

### 历史教训（旧闭环，参数与逻辑均已删除）

- **控制输出链顺序**：PID → 斜率限制 → 距离减速上限(spd_cap) → 位置死区 → 角度死区 → 到位判定 → **保存 last_y/x/w（最末尾）** → 发电机。`last_w` 在死区归零前保存导致航向振荡。
- **MIN_SPEED bang-bang**：MIN_SPEED 旁路 slew → 到位边界左右来回摆。当前 `MIN_SPEED=2.0` 只用于**转向**，不再是航向死区 boost。
- **HEADING_DEADZONE / ORIENTATION_THRESHOLD / MAX_DELTA / MAX_W_DELTA_TURN/TRANS / DECEL_K / spd_cap**：均属旧闭环，已删除。
- **周期缩放**：旧闭环 30ms→20ms 需按比例缩放 delta 限值（已删除）。当前 `Chassis_MoveByDistance` 是脉冲积分闭环（odom 用实测 elapsed_ms），`OPEN_LOOP_PERIOD_MS` 改值**不影响到位距离**；但时间标定接口（`HoldSpeedAngle`/`BlendSpeedAngle`/`MoveTurnOnce`/`DriftStraightTurn`）按 tick 跑，改周期/速度需重调这些路段参数。UART3 带宽决定周期不能压到 2ms（41B@115200≈3.6ms）。

---

## ① 共享总线写路径冲突（UART 串行化）

**根因**：单条 UART 物理总线同时只能一个发送方。多个写路径并发 → 帧损坏或 `HAL_BUSY`/`HAL_TIMEOUT` 静默丢帧。

### 当前问题

| # | 症状 | 原因 | 修复 |
|---|------|------|------|
| 1.1 | 单轮偶尔慢半拍（其它三轮正常） | 该电机 `0xF6` 速度帧迟到/被丢，复用上一周期速度 | 切 0xAA 多机同步（当前默认）后若消失即 TX 路径问题；看 `chassis_odom_tx_fail_count` |
| 1.2 | 发送超时丢帧 | `uart3WriteBuf` 等 gState 超 5ms 返回 `HAL_TIMEOUT` | 确认调用处不在长阻塞内（如机械臂动作时不要跑 `Motor_setspeed`） |
| 1.3 | 读命令挤压速度命令 | `Motor_ReadPulseSnapshot` 4×3ms 轮询读位置期间又发速度 | 读快照只在段前/段后调用，不要在 5ms 节奏内做 |
| 1.4 | 带宽瓶颈 | 每次速度命令 41B@115200≈3.6ms，5ms 周期内几乎占满 | 2ms 周期不可行；机械臂动作前 `Motor_setspeed(0,0,0)` 停车 |

### 历史教训（旧闭环）

- 500mm 跑成 1600mm：复位命令被底盘任务速度命令打断。当时用 `Set_chassis_able(unable/enable)` 挂起底盘任务，**该机制已删除**，现改为动作前停车。
- 广播读命令四电机同时回包 → 数据全乱：改为定时返回 + 发配置命令错开 2ms。当前 `Motor_ReadPulseSnapshot` 仍是逐电机轮询（间隔 3ms），不广播读。

---

## ⑥ 任务优先级 & FreeRTOS 调度

**根因**：优先级抢占改变共享资源访问时刻；同优先级任务无强制互斥时 DMA TX 会互相打断；中断优先级限制 ISR 内可调的 FreeRTOS API。

### 当前问题

| # | 症状 | 原因 | 修复 |
|---|------|------|------|
| 6.1 | 电机目标值中途被改 | 任务切换发生在 `motor1..4.target_angle` 写序列中 | `Motor_Action_Calculate_target` 已用 `__disable_irq()` 包裹 |
| 6.2 | IMU 解析被压/丢帧 | 主流程长阻塞（如 `Z_SetHeight` 4s 超时等待）与 IMU 任务抢占 | IMU_Task prio 7 已高于主流程 5；不要在主流程里做长 `Delay_ms` 硬等 IMU |
| 6.3 | 栈相关不稳定 | 任务栈不足 | 现状栈：IMU 128（浅）/ Main 512 / Start 256 / HMI 384 |

### 历史教训（旧闭环）

- 旧底盘任务(6) 抢占主任务(5)：`Motor_SetZero()` 期间读到半清零位置 → 单帧垃圾 PID。**已无独立底盘任务**，开环底盘在主流程内顺序执行，该抢占结构不存在。
- 同优先级（5）主任务与底盘互抢 DMA TX：`uart3WriteBuf` 现在自带 gState 等待 + 5ms 超时，关键帧可考虑直接寄存器轮询发送（不依赖 `HAL_GetTick()`）。

---

## 三、症状 → 类别 反查表（当前）

| 症状 | 检查 | 常见原因（类型） |
|------|------|-----------------|
| "不动了" | `chassis_odom_tx_fail_count`、`Motor_setspeed` 是否被调用 | 发送超时 / 流程未走到（①） |
| "斜着走" | 4 轮都在动？`motor_actual_pulse[4]` | 单帧丢失或机械（①/②） |
| "单轮慢半拍" | 该轮 `motor_actual_pulse` | 该轮 F6 帧迟到/丢失（①） |
| "直道弯" | 运动中 `imu.yaw` | IMU 复位时序或 Gyro_Pid（④/⑤） |
| "转向过头/不足" | `Chassis_TurnToAngle` 返回、`angle_error` | 阈值/settle/MIN_SPEED（⑤） |
| "转向绕远路" | `err_w` 计算 | 角度未归一化（⑤） |
| "Z 卡住" | `z_debug_last_timeout`、`Z_POSTION.BIT` | UART7 收不到到位帧 / 超时（④） |
| "周期超跑" | `chassis_period_overrun_count` | 调用点超过 5ms（⑤/①） |

---

## 附录 A：当前调参参数表（开环底盘）

| 参数 | 值 | 位置 |
|------|----|----|
| 控制周期 `OPEN_LOOP_PERIOD_MS` | 5ms | `motor_control.c:38` |
| 距离系数（纵/横） | 2478.5 / 2560.5 脉冲/cm | `motor_control.h:7-8` |
| 加减速 tick（<40cm / ≥40cm） | 80（400ms）/ 100（500ms） | `motor_control.h:11-13` |
| 转向阈值 / settle | 0.1° / 7 次（≈50ms） | `motor_control.c:17-18` |
| 转向 MIN_SPEED / RPM_MAX | 2.0 / 150 | `motor_control.c:19,29` |
| 转向 slew（加/减） | 2.0 / 4.0（RPM/5ms） | `motor_control.c:20-21` |
| 航向 PID `Gyro_Pid` | Kp2.1 Ki0 Kd0.5，±120 | `imu_control.c:11-15` |
| 角速度双环 | 禁用 `CHASSIS_TURN_USE_DOUBLE_LOOP=0`；保留 ANGLE_KP=3.0、RATE_KP=0.20、RATE_KI=0.15、RATE_GAIN=1.055、RATE_MAX=150、RATE_I_LIMIT=30、RATE_FILTER=0.25 | `motor_control.c:15,22-28` |
| 低速平移航向 | THRESHOLD=0.1°、MAX_SPEED=2.0、DELTA=0.5 | `motor_control.c:30-32` |
| Z 超时 `Z_MAX_TIMEOUT_MS` | 4000ms | `postion_control.c:8` |
| UART7 冷启动等待 | 500ms | `start_task.c:7` |

## 附录 B：常用 Watch 变量（Keil 直接加）

| 变量 | 观测点 |
|------|--------|
| `chassis_period_overrun_count` / `chassis_period_max_ms` | 5ms 周期是否超跑 |
| `chassis_odom_tx_fail_count` | UART3 发送失败次数 |
| `motor_check.flag_finish` | 脉冲快照完成位（0x0F） |
| `motor_actual_pulse[4]` | 4 轮实际脉冲（看单轮滞后/丢失） |
| `motor_debug_cmd_rpm_x10[4]` | 最近一次下发轮速 |
| `imu.yaw` / `imu.angular_rate` | 静止稳定性、运动漂移 |
| `car.actual_x / actual_y / actual_w` | 世界坐标、航向累计 |
| `Z_POSTION.BIT` / `z_debug_last_timeout` / `z_debug_last_wait_ms` | 机械臂到位等待 |
| `u3_debug_*` / `u7_debug_*` | UART3/UART7 接收调试计数 |

> 注意：变量必须**全局**或**文件作用域非 static** 才能在 Keil Watch 稳定显示（Keil 找不到 static 符号）。

## 附录 C：历史事故记录（旧闭环，代码均已改）

| 问题 | 类型 | 修复（现状） |
|------|------|------|
| `delaytime` 缺 volatile → Delay_ms 死循环 | ② | 加 volatile |
| `normalize_angle` static flag 共享 → 180° 转头 yaw 跳变 | ③ | 改无状态版本 |
| `Direction_Calibration_turn` 死区输出 ±1 → 转头来回摆 | ⑤ | 移除死区，统一走 Gyro_Pid |
| `flag_finish` 竞态 → 3 号电机反馈丢失（实测验证） | ③ | 快照+原子清标志 |
| 500mm 跑成 1600mm（复位命令被打断） | ① | 架构改开环，`Set_chassis_able` 删除 |
| Start_Task 栈 128 不足 → 偶发上电不启动 | ② | 栈扩到 256 |
| Telescopic_Init RXNE 与 POSTION_init DMA 冲突 | ④ | 删 RXNE，UART7 只走 DMA+IDLE |
| MIN_SPEED bang-bang 航向振荡 | ⑤ | 逻辑删除，MIN_SPEED 仅转向用 |

---

*本文档代码状态核实至 2026-08-01 当前代码；来源 `.agents/skills/embedded/SKILL.md`，旧闭环内容已标记并对照 `MOTOR/motor_control.c`、`MOTOR/imu_control.c`、`MOTOR/motor.c`、`MOTOR/postion_control.c`、`task/*.c`、`APP/task_flow.c` 修订。*
